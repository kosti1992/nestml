#### Caution: this model is not correct and only used to check if the transformation engine recognizes problems correctly. #####

neuron izhikevich_nestml:

  state:
    V_m mV = V_m_init
    u real = V_m*b/1mV
    I real = 0
  end

  equations:
    V_m' = 0.04 * V_m*V_m/1mV + 5.0 * V_m + (140 - u + I)*1mV
    u' = a*(b*V_m/1mV-u)
  end

  parameter:
    a real
    b real
    c real
    d real
	V_m_init mV
	testVariable1 boolean
	testVariable2 void
	testVariable3 string
  end
 
  input:
    spikes   <- spike
    currents <- current
  end

  output: spike

  update:
	
	integrate(V_m)	
    if V_m >= 30mV:
      V_m = c*1mV
      u += d
      emitSpike()
	end
	if a==0.2:# test whether nested statements work correct
		if b==0.02:
			V_m +=0
			V_m *=0
			V_m /=0
			V_m -=getSteps(d)
			V_m -= 0
			integrate()
		end
	end
	
  end

end